{
    "mcpServers": {
      "mapbox": {
        "command": "docker",
        "args": ["compose", "run", "--rm", "-i", "mcp-mapbox"],
        "env": {
          "MAPBOX_ACCESS_TOKEN": "${MAPBOX_ACCESS_TOKEN}"
        }
      }
    }
  }